name: vjstartups-ci

on:
  push:
    branches: [main]
    paths:
      - "backend/be2-vjstartups/**"
      - "frontend/fe2-vjstartups/**"
      - "deploy/docker-compose.vjstartups.yml"
      - ".github/workflows/vjstartups-ci.yml"
  pull_request:
    branches: [main]
    paths:
      - "backend/be2-vjstartups/**"
      - "frontend/fe2-vjstartups/**"
      - ".github/workflows/vjstartups-ci.yml"

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      # ─── BACKEND ─────────────────────────────────
      - name: Install backend dependencies
        working-directory: backend/be2-vjstartups
        run: npm install

      - name: Lint backend
        working-directory: backend/be2-vjstartups
        run: npm run lint

      # ─── FRONTEND ────────────────────────────────
      - name: Install frontend dependencies
        working-directory: frontend/fe2-vjstartups
        run: |
          npm install -g bun
          bun install

      - name: Lint frontend
        working-directory: frontend/fe2-vjstartups
        run: npm run lint

  deploy:
    needs: lint
    if: github.event_name == 'push'
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4

      - name: Build and Deploy
        run: |
          docker compose -f deploy/docker-compose.vjstartups.yml down
          docker compose -f deploy/docker-compose.vjstartups.yml up -d --build
          docker image prune -f
          docker compose -f deploy/docker-compose.vjstartups.yml ps