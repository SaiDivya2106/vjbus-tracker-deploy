name: easyfind-child-ci

on:
  push:
    branches: [main]
    paths:
      - "backend/be9-easyfind/**"
      - "frontend/fe9-easyfind-child/**"
      - "deploy/docker-compose.easy-find-child.yml"
      - ".github/workflows/easy-find-child-ci.yml"
  pull_request:
    branches: [main]
    paths:
      - "backend/be9-easyfind/**"
      - "frontend/fe9-easyfind-child/**"
      - ".github/workflows/easy-find-child-ci.yml"

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      # ─── BACKEND ─────────────────────────────────
      - name: Install backend dependencies
        working-directory: backend/be9-easyfind
        run: npm install

      - name: Lint backend
        working-directory: backend/be9-easyfind
        run: npm run lint

      # ─── FRONTEND ────────────────────────────────
      - name: Install frontend dependencies
        working-directory: frontend/fe9-easyfind-child
        run: npm install

      - name: Lint frontend
        working-directory: frontend/fe9-easyfind-child
        run: npm run lint

  deploy:
    needs: lint
    if: github.event_name == 'push'
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4

      - name: Build and Deploy
        run: |
          docker compose -f deploy/docker-compose.easy-find-child.yml down
          docker compose -f deploy/docker-compose.easy-find-child.yml up -d --build
          docker image prune -f
          docker compose -f deploy/docker-compose.easy-find-child.yml ps